<%= cp.product.name %>  
<br>
Quantité: 
<%= link_to "-", cart_product_path(cp, decrement: "true"), method: 'put', class: 'btn btn-sm btn-outline-success', remote: true %>
<span class="mx-3 fw-bold"><%= cp.quantity %></span>
<%= link_to "+", cart_product_path(cp, increment: "true"), method: 'put', class: 'btn btn-sm btn-outline-success', remote: true %>
<br>
Sous-total: <%= cp.quantity * cp.product.price %> &euro;